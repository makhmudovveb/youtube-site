import { useEffect,useState } from 'react'; import { useNavigate,useParams } from 'react-router-dom'; import { useForm } from 'react-hook-form'; import { z } from 'zod'; import { zodResolver } from '@hookform/resolvers/zod'; import type { Lesson } from '../../types'; import { getLessons,saveLesson } from '../../lib/contentService';
const schema=z.object({titleRu:z.string().min(3),titleUz:z.string().min(3),descriptionRu:z.string().min(5),descriptionUz:z.string().min(5),youtubeUrl:z.string().url(),image:z.string().url(),level:z.enum(['A1','A2','B1','B2','C1']),tags:z.string()});type Values=z.infer<typeof schema>;
export default function LessonFormPage(){const {id}=useParams();const nav=useNavigate();const [loading,setLoading]=useState(Boolean(id));const {register,handleSubmit,reset,formState:{errors,isSubmitting}}=useForm<Values>({resolver:zodResolver(schema),defaultValues:{level:'A1'}});useEffect(()=>{if(id)getLessons().then(x=>{const l=x.find(a=>a.id===id);if(l)reset({titleRu:l.title.ru,titleUz:l.title.uz,descriptionRu:l.description.ru,descriptionUz:l.description.uz,youtubeUrl:l.youtubeUrl,image:l.image,level:l.level,tags:l.tags.join(', ')});setLoading(false)})},[id,reset]);const submit=async(v:Values)=>{const item:Lesson={id:id||crypto.randomUUID(),title:{ru:v.titleRu,uz:v.titleUz},description:{ru:v.descriptionRu,uz:v.descriptionUz},youtubeUrl:v.youtubeUrl,image:v.image,level:v.level,tags:v.tags.split(',').map(x=>x.trim()).filter(Boolean),materials:[],views:0,createdAt:new Date().toISOString()};await saveLesson(item);nav('/admin')};if(loading)return <div className="p-10">Loading…</div>;return <section className="mx-auto max-w-2xl px-4 py-10"><h1 className="text-3xl font-black">{id?'Редактировать урок':'Новый урок'}</h1><p className="mt-2 text-sm text-slate-500">Картинку можно загрузить в Firebase Storage, получить download URL и вставить сюда.</p><form onSubmit={handleSubmit(submit)} className="mt-6 grid gap-4 rounded-2xl bg-white p-6 shadow-soft dark:bg-slate-900"><Input label="Название (RU)" error={errors.titleRu?.message} {...register('titleRu')}/><Input label="Sarlavha (UZ)" error={errors.titleUz?.message} {...register('titleUz')}/><Input label="Описание (RU)" error={errors.descriptionRu?.message} {...register('descriptionRu')}/><Input label="Tavsif (UZ)" error={errors.descriptionUz?.message} {...register('descriptionUz')}/><Input label="YouTube URL" error={errors.youtubeUrl?.message} {...register('youtubeUrl')}/><Input label="Image URL" error={errors.image?.message} {...register('image')}/><label>Уровень<select className="mt-1 w-full rounded-xl border bg-transparent p-3 dark:border-slate-700" {...register('level')}><option>A1</option><option>A2</option><option>B1</option><option>B2</option><option>C1</option></select></label><Input label="Теги (через запятую)" {...register('tags')}/><button disabled={isSubmitting} className="rounded-xl bg-brand-600 py-3 font-bold text-white">Сохранить</button></form></section>};
const Input=({label,error,...props}:React.InputHTMLAttributes<HTMLInputElement>&{label:string;error?:string})=><label className="text-sm font-semibold">{label}<input className="mt-1 w-full rounded-xl border bg-transparent p-3 dark:border-slate-700" {...props}/>{error&&<span className="text-xs text-rose-500">{error}</span>}</label>;
