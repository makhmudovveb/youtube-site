import { Link, NavLink, Outlet } from 'react-router-dom';
import { Moon, Sun, Menu, X } from 'lucide-react'; import { useState } from 'react'; import { useApp } from '../context/AppContext';
export function Layout() { const { lang, setLang, dark, toggleDark, t } = useApp(); const [open, setOpen] = useState(false); const links = [['/lessons',t.lessons],['/articles',t.articles],['/about',t.about],['/contacts',t.contacts]]; return <div className="min-h-screen bg-slate-50 text-slate-800 dark:bg-slate-950 dark:text-slate-100"><header className="sticky top-0 z-20 border-b border-slate-200/70 bg-white/90 backdrop-blur dark:border-slate-800 dark:bg-slate-950/90"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3"><Link to="/" className="text-xl font-black text-brand-600">Lingua<span className="text-slate-900 dark:text-white">Space</span></Link><nav className="hidden gap-5 md:flex">{links.map(([to,label]) => <NavLink key={to} to={to} className={({isActive}) => isActive ? 'text-brand-600 font-semibold' : 'hover:text-brand-600'}>{label}</NavLink>)}</nav><div className="flex items-center gap-2"><button aria-label="Theme" onClick={toggleDark} className="rounded-lg p-2 hover:bg-slate-100 dark:hover:bg-slate-800">{dark ? <Sun size={18}/> : <Moon size={18}/>}</button><button onClick={() => setLang(lang === 'ru' ? 'uz' : 'ru')} className="rounded-lg border px-2 py-1 text-sm dark:border-slate-700">{lang === 'ru' ? 'UZ' : 'RU'}</button><Link to="/login" className="hidden rounded-lg bg-brand-600 px-3 py-2 text-sm font-semibold text-white sm:block">{t.login}</Link><button className="p-2 md:hidden" onClick={() => setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></div>{open && <nav className="grid gap-3 border-t px-4 py-4 md:hidden">{links.map(([to,label]) => <Link onClick={() => setOpen(false)} key={to} to={to}>{label}</Link>)}</nav>}</header><main><Outlet/></main><footer className="mt-16 border-t border-slate-200 py-8 text-center text-sm text-slate-500 dark:border-slate-800">© {new Date().getFullYear()} LinguaSpace · English made friendly</footer></div>; }
