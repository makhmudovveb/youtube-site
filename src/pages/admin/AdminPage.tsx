import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { LogOut, Plus, Mail, Eye } from 'lucide-react'; import { useAuth } from '../../context/AuthContext'; import { getLessons,getMessages,removeLesson } from '../../lib/contentService'; import type { ContactMessage,Lesson } from '../../types';
export default function AdminPage(){const {logout}=useAuth();const [lessons,setLessons]=useState<Lesson[]>([]);const [messages,setMessages]=useState<ContactMessage[]>([]); const load=()=>{getLessons().then(setLessons);getMessages().then(setMessages)};useEffect(load,[]);return <section className="mx-auto max-w-6xl px-4 py-10"><div className="flex flex-wrap justify-between gap-4"><div><p className="font-bold text-brand-600">CMS</p><h1 className="text-3xl font-black">Админ-панель</h1></div><button onClick={logout} className="inline-flex items-center gap-2 rounded-xl border px-3 py-2 dark:border-slate-700"><LogOut size={17}/>Выйти</button></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><Stat icon={<Eye/>} label="Уроков" value={lessons.length}/><Stat icon={<Mail/>} label="Сообщений" value={messages.length}/><Stat icon={<Eye/>} label="Просмотров" value={lessons.reduce((s,x)=>s+x.views,0)}/></div><div className="mt-10 flex flex-wrap justify-between gap-3"><h2 className="text-xl font-bold">Уроки</h2><div className="flex gap-2"><Link className="inline-flex items-center gap-2 rounded-xl border px-4 py-2 font-bold dark:border-slate-700" to="/admin/articles/new"><Plus size={17}/>Статья</Link><Link className="inline-flex items-center gap-2 rounded-xl bg-brand-600 px-4 py-2 font-bold text-white" to="/admin/lessons/new"><Plus size={17}/>Новый урок</Link></div></div><div className="mt-4 overflow-x-auto rounded-2xl bg-white dark:bg-slate-900"><table className="w-full text-left text-sm"><thead className="border-b dark:border-slate-800"><tr><th className="p-4">Название</th><th>Уровень</th><th>Views</th><th></th></tr></thead><tbody>{lessons.map(x=><tr className="border-b last:border-0 dark:border-slate-800" key={x.id}><td className="p-4 font-medium">{x.title.ru}</td><td>{x.level}</td><td>{x.views}</td><td><Link to={`/admin/lessons/${x.id}`} className="mr-3 text-brand-600">Edit</Link><button onClick={async()=>{await removeLesson(x.id);load()}} className="text-rose-500">Delete</button></td></tr>)}</tbody></table></div><h2 className="mt-10 text-xl font-bold">Сообщения формы</h2><div className="mt-4 grid gap-3">{messages.length?messages.map(m=><div key={m.id} className="rounded-xl bg-white p-4 dark:bg-slate-900"><b>{m.name}</b> <span className="text-slate-500">{m.email}</span><p className="mt-1">{m.message}</p></div>):<p className="text-slate-500">Пока нет сообщений.</p>}</div></section> } function Stat({icon,label,value}:{icon:React.ReactNode;label:string;value:number}){return <div className="rounded-2xl bg-brand-50 p-5 text-brand-900 dark:bg-slate-900 dark:text-white"><div className="text-brand-600">{icon}</div><b className="mt-3 block text-2xl">{value}</b><span className="text-sm">{label}</span></div>}
