import { Link } from 'react-router-dom'; import { Heart, Eye } from 'lucide-react'; import type { Lesson } from '../types'; import { useApp } from '../context/AppContext';
export function LessonCard({ lesson }: { lesson: Lesson }) { const { lang, favorites, toggleFavorite } = useApp(); const selected = favorites.includes(lesson.id); return <article className="overflow-hidden rounded-2xl bg-white shadow-soft transition hover:-translate-y-1 dark:bg-slate-900"><img loading="lazy" src={lesson.image} alt="" className="h-44 w-full object-cover"/><div className="p-5"><div className="mb-3 flex justify-between text-xs"><span className="rounded-full bg-brand-50 px-2 py-1 font-bold text-brand-600">{lesson.level}</span><span className="flex items-center gap-1 text-slate-500"><Eye size={14}/>{lesson.views}</span></div><h3 className="text-lg font-bold">{lesson.title[lang]}</h3><p className="mt-2 line-clamp-2 text-sm text-slate-600 dark:text-slate-400">{lesson.description[lang]}</p><div className="mt-4 flex items-center justify-between"><Link className="font-semibold text-brand-600" to={`/lessons/${lesson.id}`}>{lang === 'ru' ? 'Смотреть →' : 'Ko‘rish →'}</Link><button aria-label="Favorite" onClick={() => toggleFavorite(lesson.id)} className={selected ? 'text-rose-500' : 'text-slate-400'}><Heart fill={selected ? 'currentColor' : 'none'} size={20}/></button></div></div></article>; }
