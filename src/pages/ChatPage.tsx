import { useEffect, useRef, useState } from 'react'; import { Link, Navigate, useParams } from 'react-router-dom'; import { Send, MessageCircle, ArrowLeft } from 'lucide-react'; import { useAuth } from '../context/AuthContext'; import { subscribeToChat, sendChatMessage } from '../lib/contentService'; import type { ChatMessage } from '../types'; import { Seo } from '../components/Seo';
/** Один диалог на пользователя: пользователь открывает /chat, админ — /admin/chats/:userId. */
export default function ChatPage({ adminMode = false }: { adminMode?: boolean }) { const { user, profile, isAdmin, loading } = useAuth(); const { userId } = useParams(); const chatUserId = adminMode ? userId : user?.uid; const [items, setItems] = useState<ChatMessage[]>([]); const [text, setText] = useState(''); const [error, setError] = useState(''); const bottom = useRef<HTMLDivElement>(null); useEffect(() => { if (!chatUserId) return; return subscribeToChat(chatUserId, setItems); }, [chatUserId]); useEffect(() => bottom.current?.scrollIntoView({ behavior: 'smooth' }), [items]); if (loading) return <div className="p-10">Loading…</div>; if (!user || (adminMode && !isAdmin) || !chatUserId) return <Navigate to="/" replace/>; const submit = async (event: React.FormEvent) => { event.preventDefault(); if (!text.trim()) return; try { setError(''); await sendChatMessage(chatUserId, { senderId: user.uid, senderName: profile ? `${profile.firstName} ${profile.lastName}`.trim() : user.displayName || user.email || 'User', text: text.trim() }); setText(''); } catch (e) { setError(e instanceof Error ? e.message : 'Не удалось отправить сообщение.'); } };
  return <section className="mx-auto max-w-3xl px-4 py-10"><Seo title="Чат" description="Диалог с преподавателем."/><Link to={adminMode ? '/admin' : '/profile'} className="inline-flex items-center gap-2 text-sm font-semibold text-brand-600"><ArrowLeft size={16}/>{adminMode ? 'К админ-панели' : 'К профилю'}</Link><div className="mt-4 overflow-hidden rounded-3xl bg-white shadow-soft dark:bg-slate-900"><div className="flex items-center gap-3 border-b p-5 dark:border-slate-800"><div className="rounded-xl bg-brand-50 p-2 text-brand-600"><MessageCircle/></div><div><h1 className="font-bold">{adminMode ? 'Чат с учеником' : 'Чат с преподавателем'}</h1><p className="text-sm text-slate-500">Сообщения обновляются автоматически</p></div></div><div className="h-[420px] space-y-3 overflow-y-auto bg-slate-50 p-5 dark:bg-slate-950">{items.length ? items.map(item => { const own = item.senderId === user.uid; return <div key={item.id} className={'flex ' + (own ? 'justify-end' : 'justify-start')}><div className={'max-w-[80%] rounded-2xl px-4 py-3 ' + (own ? 'bg-brand-600 text-white' : 'bg-white shadow-sm dark:bg-slate-800')}><p className="text-xs font-bold opacity-75">{item.senderName}</p><p className="mt-1 whitespace-pre-wrap">{item.text}</p></div></div>; }) : <p className="pt-20 text-center text-slate-400">Начните диалог — первое сообщение появится здесь.</p>}<div ref={bottom}/></div><form onSubmit={submit} className="border-t p-4 dark:border-slate-800"><div className="flex gap-2"><input value={text} onChange={e => setText(e.target.value)} maxLength={2000} className="min-w-0 flex-1 rounded-xl border bg-transparent px-4 py-3 dark:border-slate-700" placeholder="Напишите сообщение…"/><button aria-label="Отправить" className="rounded-xl bg-brand-600 px-4 text-white"><Send size={19}/></button></div>{error && <p className="mt-2 text-sm text-rose-500">{error}</p>}</form></div></section>; }
